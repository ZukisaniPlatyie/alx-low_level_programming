#include <stdio.h>

/**
 * main- print alphabets in lower case ff by \n
 * Return: 0 Always
 */

int main(void)
{
	char l;

	for (l = 'a'; l  <= 'z'; l++)
	{
		putchar(l);
	}

	putchar('\n');

	return (0);
}
